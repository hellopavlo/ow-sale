# OS / editor files (shouldn't be committed or deployed)
.DS_Store
Thumbs.db
.idea/
*.iml

# Leftover / non-site files
index.php

# Local-only tools — never published (public repo)
tools/

# Local engine copy for offline testing (symlink to ../sale-engine/engine)
engine-local
